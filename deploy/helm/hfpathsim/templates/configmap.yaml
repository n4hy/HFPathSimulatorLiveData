apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hfpathsim.fullname" . }}-config
  labels:
    {{- include "hfpathsim.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.api.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
